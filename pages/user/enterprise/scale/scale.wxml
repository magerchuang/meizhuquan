<!--pages/user/enterprise/scale/scale.wxml-->
<view class='container'>
  <view class='cell' wx:for="{{salary}}" wx:key="{{index}}">
     <view class='cell_item {{ selected == index ? "cell_items" : ""}}' bindtap='select' data-gm='{{item}}' data-index="{{index}}">{{item}}</view>
  </view>
</view>

